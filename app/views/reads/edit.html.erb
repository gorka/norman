<div class="mx-auto w-full md:max-w-2xl">
  <h1 class="text-2xl mb-5">Editar lectura para: <%= @read.book.title %></h1>

  <%= render partial: "common/form_errors", locals: { model: @read } %>

  <%= render partial: "form" %>
</div>

